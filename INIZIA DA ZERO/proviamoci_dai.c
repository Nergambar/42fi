#include "push_swap.h"

/* void sort(stack **a, stack **b, int size)
{
	while (size > 1)
	{
		do_pb(a, b);
		size--;
	}
	size = get_stack_size(*b);
	printf("size = %d\n", size);
	printf("%d\n", (*a)->value);
	printf("%d\n", (*b)->value);
	while (size > 1)
	{	
		if ((*a)->value < (*b)->value)
			do_rb(b);
		else
			do_pa(b, a);
		size--;
	}
} */